

# "Neuer Flug"-Button auf der Flüge-Seite

## Änderung

### `src/pages/Flights.tsx`
- In der Header-Zeile neben dem Titel `h1` einen `Button` mit `Plus`-Icon und Text aus `t("dashboard.newFlight")` hinzufügen
- Gleicher Stil wie auf dem Dashboard: `Button size="sm"` mit `Plus`-Icon, navigiert zu `/flights/new`
- Layout: `flex items-center justify-between` um Titel links und Button rechts zu platzieren

### Dateien
- **Edit**: `src/pages/Flights.tsx` — Button importieren, Header-Layout anpassen

